@model MyPortfolio.Models.User

@{
    ViewData["Title"] = "Register";
}

<style>
    .my-container {
        max-width: 800px;
    }
</style>

<main class="container px-5 my-container">
    <div class="py-5 text-center">
        <img class="d-block mx-auto mb-4" src="~/assets/bootstrap-logo.svg" alt="" width="72" height="57">
        <h2>회원가입</h2>
        <p class="lead">저희 사이트에 와주셔서 감사합니다. <br />아래의 내용을 입력하여 회원이 되어주세요😁</p>
    </div>
    <div class="row g-5">
        <div class="col">
            <h4 class="mb-3">📝기본 입력사항</h4>
            <form class="needs-validation" asp-controller="Home" asp-action="Register" novalidate>
                <div class="row g-3">
                    <div class="col-12">
                        <label asp-for="UserEmail" class="form-label">이메일 주소(ID로 사용)<span class="text-body-secondary">(필수)</span></label>
                        @* input 타입의 asp-for는 HTML로 랜더링된 뒤 id=와 name=으로 변환됨! *@
                        <input type="email" class="form-control" asp-for="UserEmail" placeholder="you@example.com" required>
                        <div class="invalid-feedback">
                            이메일 주소는 필수 입력사항입니다.
                        </div>
                    </div>

                    <div class="col-12">
                        <label asp-for="UserName" class="form-label">사용자명<span class="text-body-secondary">(필수)</span></label>
                        <div class="input-group has-validation">
                            <span class="input-group-text">&#64;</span>
                            <input type="text" class="form-control" asp-for="UserName" placeholder="홍길동" required>
                            <div class="invalid-feedback">
                                사용자명은 필수 입력사항입니다.
                            </div>
                        </div>
                    </div>

                    <div class="col-12">
                        <label asp-for="Password" class="form-label">비밀번호<span class="text-body-secondary">(필수)</span></label>
                        <input type="Password" class="form-control" asp-for="Password" placeholder="특수문자 1개 이상, 숫자, 영문자 포함 8자 이상" required>
                        <div class="invalid-feedback">
                            비밀번호는 필수 입력사항입니다.
                        </div>
                    </div>

                    <div class="col-12">
                        <label asp-for="PasswordCheck" class="form-label">비밀번호 확인<span class="text-body-secondary">(필수)</span></label>
                        <input type="Password" class="form-control" asp-for="PasswordCheck" required>
                        <div class="invalid-feedback">
                            비밀번호 확인은 필수입니다.
                        </div>
                        <span asp-validation-for="PasswordCheck" class="text-danger"></span>
                    </div>

                    <div class="col-12">
                        <label asp-for="Address" class="form-label">주소<span class="text-body-secondary">(선택)</span></label>
                        <input type="text" class="form-control" asp-for="Address" placeholder="서울시 도봉구">
                    </div>

                    <div class="col-12">
                        <label asp-for="PhoneNum" class="form-label">전화번호<span class="text-body-secondary">(선택)</span></label>
                        <input type="text" class="form-control" asp-for="PhoneNum" placeholder="010-0000-0000">
                    </div>

                </div>

                <hr class="my-4">

                <button class="w-100 btn btn-primary btn-lg" type="submit">회원가입</button>
            </form>
        </div>
    </div>
</main>

<script src="~/js/checkout.js"></script>
